# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js / build output
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Production bundles / archives (do NOT commit large zips)
functions.zip
*.zip
*.rar
*.7z

# Local env / secrets
.env
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# Temp / scratch
tmp_*
*.tmp
tmp/
.cache/

# Uploaded / processed user files (runtime workspace)
uploads/
workspace/
data/runtime/

# OpenHands / NEXUS Agent local state
openhands/state/

# Misc tool folders
.blackbox/
HF/

# Scratch probes / local experiments
_nv_probe*.mjs

# Research artifacts & paper — not part of the app (kept locally, excluded from repo)
paper/
research/
